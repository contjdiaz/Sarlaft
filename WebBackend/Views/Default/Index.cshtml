@model IEnumerable<WebBackend.Models.TipoDocumentoViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CODIGO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRE_TIPO_DOCUMENTO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CODIGO_TIPO_PERSONA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EsValido)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CODIGO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBRE_TIPO_DOCUMENTO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CODIGO_TIPO_PERSONA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EsValido)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_TIPO_DOCUMENTO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_TIPO_DOCUMENTO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_TIPO_DOCUMENTO })
        </td>
    </tr>
}

</table>
